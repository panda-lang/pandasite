{
  "name": "Panda-Programming-Language",
  "version": "1.0.0",
  "private": false,
  "license": "MIT",
  "waffle": {
    "name": "Panda Programming Language",
    "structure": "compact"
  },
  "devDependencies": {
    "static-waffle": "^0.0.22"
  },
  "scripts": {
    "export": "waffle export",
    "deploy": "git subtree push --prefix dist origin gh-pages",
    "push": "export && git push && deploy"
  }
}